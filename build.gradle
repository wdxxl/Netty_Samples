subprojects{ subProject ->
    apply plugin: 'java'
    apply plugin: 'eclipse'

    repositories{
        mavenCentral()
        jcenter()
        mavenLocal()
    }

    dependencies {
        testCompile 'junit:junit:4.12'
    }
}

wrapper {
    gradleVersion = '2.10'
}
